name: $(Build.BuildId)
jobs:
  - job: buildFlux
    displayName: 'Build Flux'
    pool:
      vmImage: ubuntu-20.04
    container: adoptopenjdk:15-jdk-hotspot
    steps:
      - script: ./gradlew build
        displayName: 'Build'
